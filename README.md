# SEDS-INDUCTION0430
So, this document describes how I tackled the tasks one by one and the difficulties I faced during the tasks.

I started with task 2 as I had some prior knowledge of Arduino and sensors, the circuit was simple enough, but the logic of the code was a bit tough. I was able to write a basic code using "If statements", though it worked it wasn't perfect. I had a particularly tough time trying to use the pushbutton like a toggle switch(I even asked in the group if I could switch it out for a toggle switch), using the last_state type of logic came in my mind but still I didn't want to use that style, after getting confirmation that it is better to use a pushbutton I got cracking on the code that uses the pushbutton like the toggle switch. After this I still faced logical errors in my code like I wasn't able to get the program to stop after reaching Wrecked state, the storm state still being activated when the anchor was dropped and also some syntax errors, so I turned to Claude AI, I told it to be my teacher and tell me what mistakes I was making but told it not to give me the fix to it. After running my code 1-2 times through Claude I understood that my logic was not fit for this requirement so taking inspiration from the task's description I started on an state machine logic, this one was perfect. After consulting Claude a few more times for fixing my logical and syntax errors, it was finally finished and worked perfectly.

The I got cracking on the first task I first opened the link to that resources provided, after seeing the video I realise that i needed some backstory to it(like i needed prior knowledge for it), so I stated the series form the first video, saw a couple of them understood the syntax, wrote a few trial codes then imported my csv file in it, then ran it, also I changed the value of depth in row 97 assuming it was a typo. To eliminate the random noise i set up an If statement inside the for loop which will check the difference between two consecutive and if it is too wayward then it will take the initial value. so after some test runs my code was ready. 

During these tasks I learnt a lot of new thing like different function of the Arduino IDE and mathplotlib etc.
